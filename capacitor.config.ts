import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'STA Components',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
